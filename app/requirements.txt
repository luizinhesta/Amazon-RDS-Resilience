# Dependências da AWS Database Lab Store (Projeto 01 — RDS Resilience Lab)
# Instalação: pip install -r requirements.txt

Flask==3.0.3
PyMySQL==1.1.1
python-dotenv==1.0.1
# gunicorn é o servidor WSGI de produção usado na EC2 (Passo 0.5).
# O servidor embutido do Flask (app.run) é de desenvolvimento e não deve
# atender tráfego real — gunicorn dá processos/worker e melhor robustez.
gunicorn==22.0.0
# boto3 é usado nas fases seguintes (S3 na Fase 4; DynamoDB no Projeto 03).
# Já incluído para preparar o reaproveitamento entre projetos.
boto3==1.34.162
# cryptography e requerido pelo PyMySQL para autenticar com o metodo padrao do
# MySQL 8 (caching_sha2_password), usado tanto no MySQL local da EC2 quanto no
# RDS. Sem este pacote a aplicacao falha ao conectar com o erro:
# "RuntimeError: 'cryptography' package is required for sha256_password or
# caching_sha2_password auth methods".
cryptography==43.0.1
